---
name: Bug report
about: Something ToolFunnel does wrong
title: ''
labels: bug
assignees: ''
---

**What happened**
A clear description of the bug.

**What you expected**
What should have happened instead.

**Setup**
- ToolFunnel version: (`npm ls toolfunnel` or git commit)
- Node version: (`node -v`)
- OS:
- Transport: stdio / HTTP (with or without OAuth)
- MCP client: (Claude Code / Claude Desktop / other + version)
- Upstream server(s) involved:

**Config**
The relevant part of your expose/config JSON (redact anything private):

```json
```

**How to reproduce**
Steps, smallest possible:
1.
2.

**Logs / output**
Any error output or relevant log lines (redact tokens!).

> ⚠️ If this is security-relevant (auth bypass, gate bypass, credential leak),
> do NOT file it here — see [SECURITY.md](../SECURITY.md).
